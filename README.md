# hipe-ocrepair-llm

Download the dataset first:
```bash
chmod +x ./scripts/*
./download_dataset.sh
```

Yo chat i wrote a simple script in `/src` go check it out: 
```bash
python src/grab_samples.py --help
```

just run `python src/grab_samples.py`
